dx=[]; 
dy=[];

filename=["a.out.txt"]
fp = fopen(filename, "r");

while ((l = fgetl(fp)) != -1)
	[x,y] = strread(l, "%f %f");
	dx = [dx;x];
	dy = [dy;y];
endwhile
	
fclose(fp);
	
fig = figure("visible", "off");
plot(dx, dy, ".", "markersize", 5, "LineWidth", 1);
print(fig, "pic", "-dpng");

